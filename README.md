# AVSR_yash
Contains the Conformer_CTC and Conformer_CG_CTC model
This repository just contains the code. For dataset and the weigths trained on LRS_2 dataset are on the machine only, due to size limit on github.
